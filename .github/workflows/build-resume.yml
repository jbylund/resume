name: Build resume

on:
  push:
    branches: [main]
    paths:
      - "**.tex"
      - resume.bib
      - makefile
      - texmf/**
      - fonts/**
      - tools/**
      - .github/workflows/build-resume.yml
  pull_request:
    paths:
      - "**.tex"
      - resume.bib
      - makefile
      - texmf/**
      - fonts/**
      - tools/**
      - .github/workflows/build-resume.yml
  # so the PDF can be refreshed when the "Last updated <month>" line rolls over
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: build-resume-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    # normally about 90 seconds. The default is 360 minutes, long enough that a
    # hung apt sits there until someone notices and cancels it by hand.
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Install TeX Live
        # normally ~45s, so 3 minutes is four times the headroom. Retries cover a
        # flaky mirror; the timeout turns a hang into a fast failure. Kept below the
        # job budget so a stuck install fails here, with a clear step name, rather
        # than as a job-level timeout.
        timeout-minutes: 3
        env:
          APT_OPTS: -o Acquire::Retries=3 -o Dpkg::Use-Pty=0
        run: |
          # The runners resolve apt through a mirrorlist -- ubuntu.sources says
          # "URIs: mirror+file:/etc/apt/apt-mirrors.txt" -- and that list starts with
          # azure.archive.ubuntu.com, which stalled on four consecutive runs. Rewrite
          # the list itself; editing ubuntu.sources does nothing, because no plain
          # mirror URL appears there.
          echo 'http://archive.ubuntu.com/ubuntu/' | sudo tee /etc/apt/apt-mirrors.txt >/dev/null
          echo "apt mirror list:"; sed 's/^/  /' /etc/apt/apt-mirrors.txt

          sudo apt-get $APT_OPTS update -qq
          # xetex: xelatex, fontspec, geometry, hyperref, xcolor -- and, through its
          # dependencies, texlive-latex-extra. Vendoring pzdr.tfm and ulem.sty in
          # texmf/ keeps texlive-fonts-recommended and texlive-plain-generic out of
          # the job; the other vendored files ride along with latex-extra regardless
          # and are kept for local builds on a minimal TeX Live. See texmf/README.md.
          #
          # No fonts-lato: Lato is vendored in fonts/lato/ and loaded by path, so the
          # text layer no longer depends on the distro's font version. python3-fonttools
          # is what the build uses to strip the hyphen cmap aliases -- installed from
          # apt rather than pip because 24.04 marks the system environment
          # externally-managed (PEP 668) and refuses a bare `pip install`.
          #
          # poppler-utils supplies pdfinfo/pdftotext for the checks below
          # texlive's postinstall precompiles a format for every engine on the
          # system: 53 of the 90 seconds this step takes. This document needs one,
          # xelatex, which takes about 3 seconds. Divert the binary to a no-op for
          # the duration of the install, then build that single format by hand.
          #
          # Divert fmtutil, not fmtutil-sys: tex-common's postinst runs
          #   FMTUTIL="fmtutil --sys --strict --no-error-if-no-engine=..."
          # so diverting the -sys wrapper misses it entirely, which is exactly the
          # mistake the previous attempt made.
          sudo dpkg-divert --local --rename --add /usr/bin/fmtutil
          sudo ln -sf /bin/true /usr/bin/fmtutil

          sudo DEBIAN_FRONTEND=noninteractive apt-get $APT_OPTS install -y --no-install-recommends \
            texlive-xetex python3-fonttools poppler-utils

          sudo rm -f /usr/bin/fmtutil
          sudo dpkg-divert --local --rename --remove /usr/bin/fmtutil
          sudo fmtutil-sys --byfmt xelatex

      - name: Build PDF
        run: |
          # No OSFONTDIR: \setmainfont loads Lato by path from build/fonts/, which
          # `make` produces from fonts/lato/ via tools/unalias-hyphen.py.
          #
          # Pin the timestamp to the start of the current month so builds are
          # byte-reproducible: the only date the resume shows is "Last updated
          # <month>, <year>", so nothing but a content change should produce a diff.
          export SOURCE_DATE_EPOCH="$(date -u -d "$(date -u +%Y-%m-01)" +%s)"
          export FORCE_SOURCE_DATE=1
          make

      - name: Check text layer
        run: |
          # The PDF can look perfect and still be unreadable to an ATS. Lato's cmap
          # aliases U+00AD and U+2010 onto the same glyph as U+002D; XeTeX embeds
          # subsets with Identity-H, so extraction inverts that mapping and resolves
          # the ambiguity to U+2010. The result was a text layer with zero ASCII
          # hyphens: neither the phone number nor any compound keyword matched a
          # literal search. tools/unalias-hyphen.py strips the aliases at build time
          # and this guards the outcome, since the failure is invisible on the page.
          python3 - <<'PY'
          import re, subprocess, sys

          text = subprocess.run(
              ["pdftotext", "-layout", "joseph_bylund.pdf", "-"],
              capture_output=True, text=True, check=True).stdout

          problems = []
          for cp, name in ((0x2010, "U+2010 HYPHEN"), (0x00AD, "U+00AD SOFT HYPHEN")):
              if (n := text.count(chr(cp))):
                  problems.append(f"{n} occurrences of {name}; expected ASCII U+002D")
          # the specific searches a recruiter or ATS actually runs
          for pattern in (r"347-829-5863", r"\d{3}-\d{3}-\d{4}", r"multi-tenant"):
              if not re.search(pattern, text):
                  problems.append(f"no match for {pattern!r}")

          for problem in problems:
              print(f"::error::text layer: {problem}")
          if problems:
              sys.exit(1)
          print(f"text layer OK: {text.count('-')} ASCII hyphens, no U+2010")
          PY

      - name: Check length
        run: |
          # The type scale is set to fill three pages almost exactly, which leaves
          # about a line of headroom -- so adding a bullet can silently produce a
          # fourth page. Fail instead, loudly. Fewer pages is fine: pruning content
          # should not need a workflow edit.
          MAX_PAGES=3
          pages="$(pdfinfo joseph_bylund.pdf | awk '/^Pages:/ {print $2}')"

          # how far down the final page the text reaches, as a percentage of the
          # text block -- reported so the slack left is visible in the run summary
          read -r height ymax <<<"$(pdftotext -bbox -f "$pages" -l "$pages" joseph_bylund.pdf - \
            | awk -F'"' '/<page /{h=$4} /yMax=/{for(i=1;i<=NF;i++) if($i ~ /yMax=/){v=$(i+1)+0; if(v>m) m=v}} END{print h, m}')"
          fill="$(awk -v y="$ymax" -v h="$height" 'BEGIN{printf "%.0f", (y - 52) / (h - 104) * 100}')"

          echo "$pages pages; final page $fill% full" | tee -a "$GITHUB_STEP_SUMMARY"

          if [ "$pages" -gt "$MAX_PAGES" ]; then
            echo "::error::Resume is $pages pages, over the $MAX_PAGES-page limit." \
                 "Either prune content or lower \\scalepct in resume_zero_start.tex."
            exit 1
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: joseph_bylund-pdf
          path: joseph_bylund.pdf
          if-no-files-found: error

      - name: Commit rebuilt PDF
        if: github.event_name != 'pull_request'
        run: |
          if git diff --quiet -- joseph_bylund.pdf; then
            echo "PDF unchanged, nothing to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # -f because .gitignore lists *.pdf, though this one is tracked
          git add -f joseph_bylund.pdf
          git commit -m "Rebuild resume PDF from ${GITHUB_SHA::7}"
          git push || { git pull --rebase && git push; }
